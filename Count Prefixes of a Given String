class Solution:
    def countPrefixes(self, words: List[str], s: str) -> int:
        ennikkai=0
        for j in words:
            for k in range(1,len(s)+1):
                if j==s[:k]:
                    ennikkai+=1
                    break
        return ennikkai

        
