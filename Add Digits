class Solution:
    def addDigits(self, num: int) -> int:
        while len(str(num))>1:
            l=list(str(num))
            k=list(map(int,l))
            num=sum(k)
        return num
        
        
