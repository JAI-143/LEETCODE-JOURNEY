class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        dabba={}
        for j in range(len(nums)):
            if target-nums[j] in dabba:
                return [dabba[target-nums[j]],j]
            else:
                dabba[nums[j]]=j 
        

        
