class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        k1=nums[:n]
        k2=nums[n:]
        ans=[]
        for j in range(n):
            ans.append(k1[j])
            ans.append(k2[j])
        return ans
