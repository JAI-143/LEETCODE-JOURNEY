class Solution:
    def maximumHappinessSum(self, happiness: List[int], k: int) -> int:
        happiness=sorted(happiness,reverse=True)
        l,a=0,0
        for j in range(k):
            if happiness[j]-a<=0:
                break
            l+=(happiness[j]-a)
            a+=1
        return l
            



        
