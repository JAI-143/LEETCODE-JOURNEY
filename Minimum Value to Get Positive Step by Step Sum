class Solution:
    def minStartValue(self, nums: List[int]) -> int:
        l=len(nums)
        dabba=[0]*(l+1)
        for j in range(1,len(nums)+1):
            dabba[j]=nums[j-1]+dabba[j-1]
        dabba.sort()
        print(dabba)
        while 0 in dabba:
            dabba.remove(0)
        if dabba:
            if dabba[0]<0:
                return abs(dabba[0])+1
            else:
                return 1
        else:
            return 1
