class Solution:
    def findArray(self, pref: List[int]) -> List[int]:
        tamil=[pref[0]]
        for j in range(len(pref)-1):
            tamil.append(pref[j]^pref[j+1])
        return tamil
        
