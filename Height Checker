class Solution:
    def heightChecker(self, heights: List[int]) -> int:
        l=sorted(heights)
        c=0
        for j in range(len(heights)):
            if heights[j]!=l[j]:
                c+=1
        return c
        
