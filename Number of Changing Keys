class Solution:
    def countKeyChanges(self, s: str) -> int:
        c=0
        for j in range(len(s)-1):
            if s[j].lower()!=s[j+1].lower():
                c+=1
        return c
        
