class Solution:
    def kidsWithCandies(self, candies: List[int], extraCandies: int) -> List[bool]:
        ec=[f+(extraCandies) for f in candies]
        l=max(candies)
        ans=[k>=l for k in ec]
        return (ans)
        
